import SeoPage from "./SeoPage";

export default function HolidaysTurkey() {
  return (
    <SeoPage
      title="Holiday packages in Turkey"
      subtitle="Antalya, Istanbul, Bodrum — hotel packages and seasonal offers."
      bullets={[
        "Packages with hotel options",
        "Family and couples offers",
        "Ask for current dates & availability",
      ]}
    />
  );
}
